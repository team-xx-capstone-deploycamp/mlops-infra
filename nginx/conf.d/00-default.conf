server {
    listen 80;
    server_name _;
    return 444;
}

server {
    listen 8081;

    location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        allow 172.16.0.0/12;  # Docker networks
        deny all;
    }
}